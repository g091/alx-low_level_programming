#include <stdio.h>

/**
 * main - Entry point
 * betty style doc for function goes there
 * Return: Always return zero (0)
 */

int main(void)
{
	int n;

	for (n = 0; n < 10 ; n++)
	putchar(n + '0');
	putchar('\n');
	return (0);
}
