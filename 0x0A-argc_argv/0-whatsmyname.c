#include <stdio.h>
/**
 * main -  writes it's name
 * @argc: arguement count
 * @argv: argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}
