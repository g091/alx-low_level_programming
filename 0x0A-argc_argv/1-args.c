#include <stdio.h>

/**
 * main - prints the no. of arguenments passed to it
 * @argc: arguemnet count
 * @argv: argument vector
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}
